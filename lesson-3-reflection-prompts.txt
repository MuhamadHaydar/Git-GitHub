When would you want to use a remote repository rather than keeping all your work
local?

    Every time, i want to keep my files in safe place like github instead of 
    placing whole of it in local.
    
   
Why might you want to always pull changes manually rather than having Git
automatically stay up-to-date with your remote repository?

    because it may better for some reasons such as:
    when i am working on a specfied feauture of application, at the same time another co-worker from the team update an part of application, it will be automatically update for myself and whole my works will be gone.

Describe the differences between forks, clones, and branches.  When would you
use one instead of another?

    fork: is clone the repository from github to github.
    clone: is clone the repository from github to local machine or from local place to another same one.
    branche: is an specific part to refer commits in one place.

What is the benefit of having a copy of the last known state of the remote
stored locally?

    having a copy of remote have some benifets, one of them is that u can work on it using git and make commits over it.

How would you collaborate without using Git or GitHub?  What would be easier,
and what would be harder?

    collaboration without git hub would be much hader than with it, Git & GitHub makes coding much easier than before.

When would you want to make changes in a separate branch rather than directly in
master?  What benefits does each approach have?

    when i want to collaborate with othre people into add a specific feauture.
