What happens when you initialize a repository? Why do you need to do it?

  because we need to save commits and their status.

How is the staging area different from the working directory and the repository?
What value do you think it offers?

  it is just like an connection between working directory and the repository, When we 
  need to add our files we have to add to the staging area first.
  it gives us an relation between these two files.

How can you use the staging area to make sure you have one commit per logical
change?

  We use staging area to add files for each commits.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help? 

  branches would be helpful when we want to make another version or add an expermental feauters to 
  application, branches such as saying: here is a new way when u have application then add feauters.

How do the diagrams help you visualize the branch structure?

  the diagrams are pretty powerful to understand the commits rechable.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

  we can add two branches together by checking out the master then merge it with coins(ex)
  then, it well add new commit ass merge branch coins. then we have to delete the lable of 
  coins, which whole of commits in coin well be part of mastert.

What are the pros and cons of Git's automatic merging vs. always doing merges
manually?

  if it is automatically, commits will be countless, but when it is manually, it well be better
  anytime we want, we can make a commit.
   

